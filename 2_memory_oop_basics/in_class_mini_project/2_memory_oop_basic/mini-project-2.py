import numpy as np

# Create code to simulate recall 1) across list lengths, 2) with and without interference, and 3) visualize forgetting curves



# Generic model
def predict_recall_prob ():
    models=['baseline', 'interference']
    #baseline is recall prob as a function of list length
    #interference is same as baseline plus interference term

def experiment(n_trials):
    condition=['baseline', 'interference']
    trial.decider=np.random.choice(
        a=condition, size=n_trials, replace=True, p=[.5,.5])
    design=[]


all_trials=[]



# Run an experiment dividing participants into control and baseline conditions


